# Flask / process
HOST=0.0.0.0
PORT=5001
AUTO_INIT=true
PLANT_DATA_PATH=/app/pants_filtered.csv

# CORS — comma-separated origins, or * for open (dev only)
CORS_ORIGINS=*
CORS_CREDENTIALS=false

# Rate limiting (flask-limiter syntax)
RATE_LIMIT=60 per minute
RATELIMIT_STORAGE_URI=memory://
